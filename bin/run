#!/usr/bin/env zsh
set -eu -o pipefail

# TODO --server.folderWatchList TEXT
# but it should already watch every file from the pwd and its subdirectories
# (recursively?)
uv run streamlit run --server.runOnSave True state.py
